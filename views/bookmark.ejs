<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>부동산 도우미</title>
  <meta name="keywords" content="부동산 도우미" />
  <meta name="description" content="부동산 업무에 도움을 드립니다. 특약사항, 질문답변, 매물찾기, 부동산뉴스, 일정관리..." />

  <!-- 브라우저 css 기본스타일 초기화 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reset-css@5.0.2/reset.min.css">

  <!-- ajax 사용을 위한 제이쿼리 -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  
  <!-- css 링크  -->
  <link rel="stylesheet" href="../css/main.css">
  <link rel="stylesheet" href="../css/header.css">
  <link rel="stylesheet" href="../css/bookmark/bookmark.css">

  <!-- js 링크  -->
</head>

<body>

  <!-- ===== header ===== -->
  <header>
    <%- include('header', {user:user}) %>
  </header>

  <div id="contents">

    <section class="main">
      <div class="grid">
        <% for(let i=0; i<data.length; i++) { %>
          <div class="CardContainer">
            <div class="CardBox">
              <div class="card">
                <a href="<%= data[i].link %>" target="_blank">
                  <img class="bookmark_image" src="../images/bookmarks/<%= data[i].image %>" alt="<%= data[i].alt %>">
                  <div class="container">
                    <p id="bookmark_title"><%= data[i].title %></p> 
                    <p id="bookmark_description"><%= data[i].description1 %></p>
                    <p id="bookmark_description"><%= data[i].description2 %></p> 
                    <% if(data[i].description2 == null) { %>
                      <p id="bookmark_description"><br></p> 
                    <% } %>
                  </div>
                </a>
              </div>
            </div>
          </div>
        <% } %>
      </div>
    </section>   

  </div>    
</body>
</html>